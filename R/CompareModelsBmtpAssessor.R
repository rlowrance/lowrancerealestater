CompareModelsBmtpAssessor <- function(scenario, testing.period, data) {
    # return list of Models and other objects for the scenario and testing period
    # $Model : list of Models(data, training
    # return a list of Models such that the model with the lowest RMSE in the testing.period 

    # return best # training days for the scenario in the testing period
    # use cross validation to select this
    # OR
    # return Model list, description list, Assess function (BETTER)
}
